package com.qualcomm.qce.allplay.controllersdk;

public enum ConnectionState {
    DISCONNECTED, CONNECTING, CONNECTED, WIFI_OFF,
    CONNECTING_TIMEOUT, DISCOVERING_DEVICE_TIMEOUT, CONNECTING_ERROR
}
